{
  "name": "newtab.jonathanearl.io",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.5.1",
    "react-dnd": "^5.0.0",
    "react-dnd-html5-backend": "^5.0.1",
    "react-dom": "^16.5.1",
    "react-icons": "^3.0.5",
    "react-scripts": "1.1.5",
    "styled-components": "^3.4.6"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "rm -rf ./build && aws s3 rm s3://newtab.jonathanearl.io/ --recursive && yarn build && aws s3 cp ./build s3://newtab.jonathanearl.io/ --recursive"
  }
}
